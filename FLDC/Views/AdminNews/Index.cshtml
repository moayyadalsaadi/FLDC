@model IEnumerable<Graduation_Project.Models.News>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div class="text-right">
    <br />
    <h2>الاخبار</h2>
    <hr />

    <p>
        @Html.ActionLink("جديد", "Create", null, new { @class = "btn btn-primary" })
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NewsContent)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NewsContent)
                </td>
                <td>
                    @Html.ActionLink("تعديل", "Edit", new { id = item.NewsId }) |
                    @Html.ActionLink("التفاصيل", "Details", new { id = item.NewsId }) |
                    @Html.ActionLink("حذف", "Delete", new { id = item.NewsId })
                </td>
            </tr>
        }

    </table>
</div>